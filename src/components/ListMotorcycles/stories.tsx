import { Story, Meta } from '@storybook/react/types-6-0';
import ListMotorcycles from '.';

export default {
  title: 'ListMotorcycles',
  component: ListMotorcycles
} as Meta

export const Default: Story = () => <ListMotorcycles />;
