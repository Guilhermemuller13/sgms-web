import { Story, Meta } from '@storybook/react/types-6-0';
import ListImagesPreview from '.';

export default {
  title: 'ListImagesPreview',
  component: ListImagesPreview
} as Meta

export const Default: Story = () => <ListImagesPreview />;
